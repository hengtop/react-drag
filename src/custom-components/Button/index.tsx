import { ButtonWrapper } from "./style";

import styled from "@emotion/styled";

const Button = (props) => {
  return <ButtonWrapper {...props}>index</ButtonWrapper>;
};

const PreButtom = styled(Button)`
  color: #bfa;
`;

export default PreButtom;
